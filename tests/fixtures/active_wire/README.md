# Active Strategy wire fixtures

These deterministic hexadecimal FlatBuffers payloads are generated by
`scripts/generate/generate_active_wire_fixtures.py`. Rust and Python tests must decode
the same checked-in bytes. A root belongs here only while it is part of the
active Strategy transport surface; planned view schemas are excluded.
