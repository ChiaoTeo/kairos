# Reference connector example

Connector examples belong under `examples/connectors/`.

This directory is intentionally minimal. It keeps user-facing examples aligned
with the current connector boundary naming and prevents new examples from using
the removed `examples/adapters/` layout.
