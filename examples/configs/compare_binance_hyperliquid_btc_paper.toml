[launch]
id = "example-compare-binance-hyperliquid-btc-paper"
mode = "paper"
strategy = "examples.strategies.compare_binance_hyperliquid_btc:strategy"

# Paper mode still uses the live public market connections because the
# strategy subscribes dynamically. It does not refresh a private Binance
# account during startup.
[account]
ref = "paper_btc"

[paper]
