[launch]
id = "example-account-context-live"
mode = "live"
strategy = "examples.strategies.account_context_live:strategy"

[strategy.params]
account = "binance_zhaoqian888666"
segment = "spot"

[live]

# This example reads the account view only. It does not submit orders.

[accounts.main]
# Replace this with the alias created by `account connect` below.
ref = "binance_zhaoqian888666"
# Only the spot wallet is needed for this example. Add usd_m_futures,
# options, or another segment explicitly when a strategy needs it.
segments = ["spot"]
trade = false
