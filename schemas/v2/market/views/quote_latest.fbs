include "../../common/metadata.fbs";
include "../types/quote.fbs";

namespace kairos.market.v2;

table LatestQuote {
  value:Quote (required);
  source_event_id:string (required);
}

table QuoteLatestView {
  metadata:kairos.common.v2.ViewMetadata (required);
  quote:LatestQuote (required);
}

root_type QuoteLatestView;
file_identifier "MLQ2";
