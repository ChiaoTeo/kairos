include "../../common/metadata.fbs";
include "../types/order_book.fbs";

namespace kairos.market.v2;

table LatestOrderBook {
  value:OrderBookSnapshotValue (required);
  synchronized:bool;
  source_event_id:string;
}

table OrderBookLatestView {
  metadata:kairos.common.v2.ViewMetadata (required);
  book:LatestOrderBook (required);
}

root_type OrderBookLatestView;
file_identifier "MLO2";
