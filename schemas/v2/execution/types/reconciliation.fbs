namespace kairos.execution.v2;

enum ReconciliationReason:ubyte {
  UNSPECIFIED = 0,
  UNKNOWN_REMOTE_ORDER = 1,
  ORDER_STATE_DIVERGED = 2,
  FILL_STATE_DIVERGED = 3,
  ACCOUNT_STATE_DIVERGED = 4,
  COMPENSATION_EXHAUSTED = 5,
  EVENT_GAP = 6
}
