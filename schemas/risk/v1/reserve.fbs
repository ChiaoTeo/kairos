include "../../common/v1/header.fbs";
include "types.fbs";

namespace kairos.risk.v1;

table ReserveRiskRequest {
  header:kairos.common.v1.MessageHeader (required);
  reservation_id:string (required);
  request_id:string (required);
  created_at_unix_nanos:ulong;
  usages:[Usage] (required);
}

root_type ReserveRiskRequest;
file_identifier "RRR1";
