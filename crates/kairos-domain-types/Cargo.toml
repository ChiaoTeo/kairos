[package]
name = "kairos-domain-types"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Shared, infrastructure-free value objects for trading domains"

[dependencies]
serde = { version = "1", features = ["derive"] }
rust_decimal = { version = "1", default-features = false, features = ["serde"] }

[dev-dependencies]
serde_json = "1"
