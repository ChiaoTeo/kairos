[package]
name = "kairos-capital-contract"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Capital module cross-process command, snapshot, and event contract"

[lib]
name = "kairos_capital_contract"
path = "src/lib.rs"

[dependencies]
serde.workspace = true
serde_json.workspace = true

